[project]
name = "mc-tracker-bot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "discord.py>=2.3.2",
    "mcstatus>=11.1.1",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
